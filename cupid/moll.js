// Function definition with parameters and return statement
function band(name, genre) {
    return `The band ${name} plays ${genre} music.`;
}

// Calling the function with arguments and storing the return value
let description = band("Led Zeppelin", "rock");

console.log(description); // Output: "The band Led Zeppelin plays rock music."
